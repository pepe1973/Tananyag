# Nincs behúzás.
for i in range(10):
	# 1 tab-os behúzás.
	if i % 2 == 0:
		# 2 tab-os behúzás.
		k = i / 2
		print(k)
	# 1 tab-os behúzás.
	else:
		# 2 tab-os behúzás.
		l = (i + 1) / 2
		print(l)
			